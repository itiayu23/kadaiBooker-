<%= render 'books/list', book: @books, user: @user %>
<div class="container">
  <div class="row">
    <div class="col-md-8 offset-md-1">
      
   
<h2>Book detail</h2>
<table class="table">
  <tbody>
 <tr> 
  <td>
    <%= image_tag @user.get_profile_image(100,100) %>
  <br>
  <%= link_to @user.name, user_path(current_user.id) %>
  </td>
  <td><%= link_to book_path(@book.id) do %>
    <%= @book.title %>
    <% end %></td>
  <td><%= @book.opinion %></td>
  <td><%= link_to edit_book_path(@book.id), class: "btn btn-sm btn-success" do %>
  Edit<% end %></td>
  <td><%= link_to book_path(@book.id), method: :delete, "data-confirm" => "本当に消しますか？", class: "btn btn-sm btn-danger" do %>
  Destroy<% end %></td>
  
  </tr>
 </tbody>
</table>
 </div>
  </div>
</div>
